package com.example.calculator.model;

public interface Icalculator {
    /**
     * Returns the result according to the class that implements the interface
     * @param num1
     * @param num2
     * @return
     */
    public Result calculate(double num1,double num2);

}
